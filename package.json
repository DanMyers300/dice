{
  "name": "dice",
  "private": true,
  "scripts": {
    "build": "bun build src/main.ts --outdir dist && cp index.html dist/index.html && sed -i 's|./src/main.ts|./main.js|' dist/index.html"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/three": "^0.183.1",
    "prettier": "^3.8.1",
    "three": "^0.183.1"
  }
}
